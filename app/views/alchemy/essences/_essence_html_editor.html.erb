<% cache(content) do %>
<div class="content_editor essence_html" data-content-id="<%= content.id %>" id="<%= content.dom_id %>">
  <%= label_and_remove_link(content) %>
  <%= text_area_tag(
    content.form_field_name,
    content.ingredient
  ) %>
</div>
<% end %>