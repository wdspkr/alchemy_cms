<%- cache(content) do -%>
<%- options = local_assigns.fetch(:options, {}) -%>
<%- plain_text = !!value_from_settings_or_options(content, options, :plain_text) -%>
<%= raw content.essence.send(plain_text ? :stripped_body : :body) -%>
<%- end -%>